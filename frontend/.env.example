# URL base de la API. En desarrollo usa el proxy de Vite (vacío o relativo).
# Si el backend corre en otra URL, defínela aquí.
VITE_API_URL=/api

# URL absoluta pública del sitio (para SEO, canonical y Open Graph).
VITE_SITE_URL=http://localhost:5173